/**
 * The @react-native-vector-icons/* packages link their own font files through
 * autolinking, so nothing needs listing for them here.
 *
 * `assets` is for custom fonts you drop into src/assets/fonts. After adding
 * one, run `npx react-native-asset` and rebuild the native apps.
 */
module.exports = {
  project: {
    ios: {},
    android: {},
  },
  assets: ['./src/assets/fonts'],
};
